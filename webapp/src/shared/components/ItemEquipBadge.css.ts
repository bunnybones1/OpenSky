import { style } from '@vanilla-extract/css'

export const ItemEquipBadgeStyle = style({
  width: '55%',
  right: '-8%'
})

export const ItemEquipBadgeTooltipWrapper = style({
  width: '29%'
})

export const ItemEquipBadgeTooltip = style({
  paddingTop: 'calc(1/1 * 100%)',
  left: '-74%',
  top: '47%'
})

export const BadgeImage = style({
  transition: 'opacity 0.2s ease-out'
})
