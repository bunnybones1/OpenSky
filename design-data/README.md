# Design Data

This package contains no code designed to be run at runtime.
It contains data used by other packages, exposed as a `json` file,
and it contains tools to be used at development/CI time to validate and work with that data.
