import { getUrlColor } from './utils/locationColors'

// prettier-ignore
const queryParams = {
  bgColor: getUrlColor('bgColor'),
  color1: getUrlColor('color1'),
  color2: getUrlColor('color2')
}

export default queryParams
